# Keep data/fred_master.parquet in the repo (committed by GitHub Actions daily)
# Exclude individual raw series files to keep repo size manageable
data/raw/

# Python
__pycache__/
*.pyc
*.pyo
.env

# Streamlit local config
.streamlit/secrets.toml
